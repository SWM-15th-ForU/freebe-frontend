import submitStyles from "./submit.css";

interface ScheduleModalProps {
  onClickBackground: () => void;
}

const ScheduleModal = ({ onClickBackground }: ScheduleModalProps) => {
  return <div />;
};

export default ScheduleModal;
