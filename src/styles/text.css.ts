import { styleVariants } from "@vanilla-extract/css";

export const texts = styleVariants({
  body1: {
    fontSize: "15px",
    fontWeight: 400,
  },
  button1: {
    fontSize: "18px",
    fontWeight: 600,
  },
});
